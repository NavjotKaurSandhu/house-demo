# House

### Setting Up a Project

#### Install packages:

```

Run `cd <project name>`

Run `npm install`

```

#### Start development server

```

Run `npm run start` 

Navigate to `http://127.0.0.1:8080/`.

```